# Changelog

## 2026-06-05
- Updated the remote timer background catalog with calibrated `defaultOffsetY` values for all 16 current presets.
- Added the remote timer background preset catalog under `assets/timer-backgrounds/`, including curated legacy-ID metadata, generated WebP thumbnail/phone variants, a committed `catalog.v1.json`, and CI validation for stale output.
